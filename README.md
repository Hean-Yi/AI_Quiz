# AI Quiz - 智能测验生成器

AI Quiz 是一个全栈应用程序，利用 AI 技术将 PDF 学习资料转化为互动式测验。它包含一个 Vue 3 前端（可通过 Capacitor 打包为 Android 应用）和一个基于 OpenAI/LangChain 的 Express/Node.js 后端。

## 🌟 功能特性

*   **PDF 解析**: 支持上传 PDF 课件或文档，自动提取文本内容。
*   **AI 智能出题**: 根据文档内容自动生成单选题、判断题和简答题。
*   **RAG (检索增强生成)**: 
    *   使用 **LangChain** 和 **HNSWLib** 构建本地向量知识库。
    *   通过语义检索（Semantic Search）获取文档中最相关的上下文，确保题目生成的准确性。
    *   支持引用原文页码 `[Page X]`，方便溯源。
*   **交互式测验 UI**:
    *   动态题目卡片设计，流畅的动画效果。
    *   实时进度追踪与成绩分析。
    *   **错题本**: 自动记录错题，支持随时复习。
*   **移动端优先**: 专为移动设备优化，支持通过 Capacitor 打包为 Android APK。
*   **深度链接 (Deep Linking)**: 支持从其他应用（如微信、文件管理器）直接打开 PDF 文件并跳转到 AI Quiz 进行解析。

## 🛠️ 技术栈

### 客户端 (Client)
*   **框架**: Vue 3 + Vite
*   **状态管理**: Pinia
*   **样式**: Tailwind CSS
*   **移动端运行时**: Capacitor (Android)

### 服务端 (Server)
*   **运行时**: Node.js + Express
*   **AI/LLM**: LangChain.js + OpenAI API (兼容 DeepSeek 等 OpenAI 格式接口)
*   **PDF 处理**: pdf-parse
*   **向量存储**: HNSWLib (本地文件系统存储，无需外部向量数据库)

## 📂 项目结构

```
AI_Quiz/
├── client/                 # Vue 3 前端项目
│   ├── src/
│   │   ├── views/          # 页面组件 (Home, Quiz, Mistakes...)
│   │   ├── stores/         # Pinia 状态管理
│   │   └── components/     # 通用组件
│   └── android/            # Android 原生工程文件
├── server/                 # Express 后端项目
│   ├── services/           # 核心业务逻辑
│   │   ├── pdfService.js   # PDF 解析
│   │   ├── ragService.js   # 向量检索与存储 (RAG)
│   │   └── aiService.js    # AI 对话与题目生成
│   ├── data/               # 向量索引与 Prompt 模板
│   └── uploads/            # 临时文件存储
└── build_apk.ps1           # Android 打包脚本
```

## 🧩 核心工作流

1.  **文档处理**: 用户上传 PDF -> 后端使用 `pdf-parse` 提取文本 -> `RecursiveCharacterTextSplitter` 切分文本块。
2.  **向量化**: 文本块通过 Embedding 模型转换为向量 -> 存入 `HNSWLib` 本地索引。
3.  **题目生成**: 用户发起请求 -> 系统根据题型构造 Query -> 检索 Top K 相关文本块 -> 组装 Prompt 发送给 LLM -> 返回 JSON 格式题目。

## 🚀 快速开始

### 前置要求
*   Node.js (v18+)
*   Android Studio (用于构建 APK)

### 安装步骤

1.  **克隆仓库**
    ```bash
    git clone https://github.com/Hean-Yi/AI_Quiz.git
    cd AI_Quiz
    ```

2.  **配置服务端**
    ```bash
    cd server
    npm install
    # 启动开发服务器 (默认端口 3000)
    npm run dev
    ```

3.  **配置客户端**
    ```bash
    cd client
    npm install
    # 启动前端开发服务器
    npm run dev
    ```

### 构建 Android APK

1.  **构建前端资源**
    ```bash
    cd client
    npm run build
    ```

2.  **同步 Capacitor 配置**
    ```bash
    npx cap sync
    ```

3.  **构建 APK**
    你可以使用提供的 PowerShell 脚本 (Windows):
    ```powershell
    ./build_apk.ps1
    ```
    或者打开 Android Studio 进行构建:
    ```bash
    npx cap open android
    ```

## 📄 许可证

[MIT](LICENSE)
